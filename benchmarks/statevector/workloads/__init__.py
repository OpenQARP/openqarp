"""Thin shims discovered by filename: <family>_<stack>.py.

Each binds one family kernel from _families to one stack adapter, so the stack
is carried by the filename and the measurement protocol is shared.
"""
